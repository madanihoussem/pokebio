{% extends 'base.html.twig' %}

{% block title %}Produits index{% endblock %}

{% block body %}
<main>
    <nav class="navbar navbar-expand-xl navbar-dark bg-dark p-2" aria-label="Sixth navbar example">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">PokéBio</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample06" aria-controls="navbarsExample06" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarsExample06">
        <ul class="navbar-nav me-auto mb-2 mb-xl-0">
         <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{path('dashboard')}}">Accueil</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="{{path('produits_index')}}">Produits</a>
          </li>
          <li class="nav-item">
            <a class="nav-link " href="{{path('contacts_index')}}">Contacts</a>
          </li>
          <li class="nav-item">
            <a class="nav-link " href="{{path('blog_index')}}">Blog</a>
          </li>
          <li class="nav-item">
            <a class="nav-link " href="{{path('user_index')}}">User</a>
          </li>
          <li class="nav-item">
            <a class="nav-link " href="{{path('home')}}">Site</a>
          </li>
          
        </ul>
        <form>
          <a href="{{path('app_logout')}}" type="button" placeholder="Search" aria-label="Search">Déconnexion</a>

        </form>
      </div>
    </div>
  </nav>
    <div class="container mt-5">
    <h1>Produits index</h1>
    <a href="{{ path('produits_new') }}">Create new</a>
    <table class="table border">
        <thead class="table-dark">
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for produit in produits %}
            <tr>
                <td>{{ produit.id }}</td>
                <td>{{ produit.nom }}</td>
                <td>{{ produit.description }}</td>
                <td>{{ produit.prix }}</td>
                <td>
                    <a href="{{ path('produits_show', {'id': produit.id}) }}">show</a>
                    <a href="{{ path('produits_edit', {'id': produit.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    </div>
    </main>
{% endblock %}
